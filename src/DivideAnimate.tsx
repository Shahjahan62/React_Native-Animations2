import { View, Text, Animated } from "react-native";
import React from "react";

const DivideAnimate = () => {
  const animated = new Animated.Value(0);
  return (
    <View>
      <Text>This is new commit</Text>
    </View>
  );
};

export default DivideAnimate;
